@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <title>Добавление человека</title>
    <style>
        table.jewel {
            
        }

        th {
            background: #87CEEB; /* Цвет фона */
            color: #fff; /* Цвет текста */
            text-align: left; /* Выравнивание по левому краю */
            
            box-shadow: 0 0 15px 4px rgba(0,0,0,0.06);
            border-radius: 10px;
        }

        tr.odd {
            background: #FFEFD5; /* Цвет фона */
            border: 1px solid grey; /* Границы ячеек*/
        }
        input[type=text] {
            padding: 10px;
            border: 0;
            box-shadow: 0 0 15px 4px rgba(0,0,0,0.06);
            border-radius: 10px;
        }
        input[type=submit] {
            padding: 10px;
            border: 0;
            box-shadow: 0 0 15px 4px rgba(0,0,0,0.06);
            border-radius: 10px;
        }
        input[type=reset] {
            padding: 10px;
            border: 0;
            box-shadow: 0 0 15px 4px rgba(0,0,0,0.06);
            border-radius: 10px;
        }
    </style>
</head>
<body>
    <div>
        <h3>Форма добавления</h3>
        <p>Для добавления заполните следующие поля:</p>
        <form method="post">
            <table class="jewel">
                <tr>
                    <th>Фамилия</th>
                    <td><input type="text" name="Surname" /> </td>
                </tr>
                <tr>
                    <th>Имя</th>
                    <td><input type="text" name="Name" /></td>
                </tr>
                <tr>
                    <th>Отчество</th>

                    <td><input type="text" name="Patronymic" /></td>
                </tr>
                <tr>
                    <th>Дата рождения</th>
                    <td><input type="text" name="BirthDate" placeholder="Формат 01.01.2001" maxlength="10"/></td>
                </tr>
                <tr>
                    <th>Снилс</th>
                    <td><input type="text" name="Snils" placeholder="Формат 123-456-789-01" maxlength="14"/></td>
                </tr>
                <tr><td><input type="submit" value="Отправить" style="background-color:blanchedalmond"/> </td><td><input type="reset" value="Очистить" style="background-color:red;"/></td></tr>
            </table>
        </form>
    </div>
</body>
</html>